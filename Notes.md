# Variants

- Add a constraint to make images smaller as time progresses
- Do a "square-by-square" composition. The shapes are placed looking only at one patch of the image
- After the base is "painted", start adding smaller shapes

# How scale works

a sacle of 1.0 scales to the same size as the output image, a scale <1 increases the size and >1 reduces the size